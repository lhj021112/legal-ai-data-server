services:
  - type: web
    name: legal-ai-data-server
    env: python
    rootDir: data_server
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
